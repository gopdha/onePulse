# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
env/
venv/
.venv/
ENV/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
.coverage
htmlcov/

# ===== Node.js (MCP servers, tooling) =====
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# ===== Secrets & credentials =====
# Broad on purpose: this project has a real history of credential exposure
# incidents during interactive development. Nothing matching these patterns
# should ever be committed, even temporarily.
.env
.env.*
!.env.example
*.pem
*.key
*credentials*
*secrets*
*.pfx
kubeconfig
*.kubeconfig
azure-pipelines-secrets.yml

# ===== Infrastructure as Code state =====
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# ===== Local dev artifacts =====
# Report Rendering's local output during development, before wiring to
# Azure Blob Storage — matches singleSlide_demo's own convention of never
# committing generated report artifacts.
*.pptx
# Scoped exception: hand-authored architecture diagrams living alongside
# docs/build-journey/'s own documentation set are not generated pipeline
# output — the blanket rule above still protects every real generated
# report artifact everywhere else in the repo.
!docs/build-journey/*.pptx
*.pdf
local_output/

# Full-fidelity per-run pipeline logs (Task 32) — one real log file per
# Generate action (logs/<project>_<timestamp>.log), a per-run artifact,
# not code.
logs/

# ===== Docker =====
*.log

# ===== IDE =====
.vscode/
.idea/
*.swp
*.swo

# ===== OS =====
.DS_Store
Thumbs.db
